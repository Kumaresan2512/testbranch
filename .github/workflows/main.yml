name: "exampleReport"
on: [push]

jobs:
  # make sure the action works on a clean machines without building
  test_aws_response:
     runs-on: ubuntu-latest
     steps:
      - name: Publish Report
        run : 
           echo "Hello"
           echo "Kumaresan"
